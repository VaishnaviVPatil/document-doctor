import type { DocumentChecklist } from "../../checklists";

const CEAC = "https://ceac.state.gov/genniv/";
const SEVIS = "https://www.fmjfee.com";
const STUDY_US = "https://studyinthestates.dhs.gov";

const US_VISA_PHOTO = {
  maxBytes: 240 * 1024,
  mimeTypes: ["image/jpeg"],
  image: {
    minWidth: 600,
    minHeight: 600,
    maxWidth: 1200,
    maxHeight: 1200,
    aspectRatio: 1,
    description: "2×2 in (600×600 to 1200×1200 px, square), JPEG ≤ 240 KB",
  },
} as const;

export const VISA_US_F1: DocumentChecklist = {
  id: "visa-us-f1",
  title: "US F-1 Academic Student Visa",
  category: "visa",
  country: "United States",
  estimatedDays: 30,
  items: [
    {
      id: "i-20",
      label: "Form I-20 from SEVP-certified school",
      description: "Signed by the DSO and the student.",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf"],
      officialSourceUrl: STUDY_US,
      validationHints: [
        "Original signature from Designated School Official (DSO)",
        "Student's signature at the bottom",
        "SEVIS ID starts with 'N' and is visible",
      ],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf"] },
    },
    {
      id: "sevis-fee",
      label: "I-901 SEVIS fee receipt ($350)",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      officialSourceUrl: SEVIS,
      validationHints: ["Receipt matches SEVIS ID on I-20", "Shows $350 fee paid"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "ds-160",
      label: "DS-160 confirmation page",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf"],
      officialSourceUrl: CEAC,
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf"] },
    },
    {
      id: "photo",
      label: "Digital visa photo (2×2 in square)",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["image/jpeg"],
      validation: US_VISA_PHOTO,
    },
    {
      id: "passport",
      label: "Passport valid 6+ months beyond stay",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "mrv-fee",
      label: "MRV visa fee receipt ($185)",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "appointment",
      label: "Visa interview appointment confirmation",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "admission-letter",
      label: "Admission / offer letter from the school",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "academic-transcripts",
      label: "Academic transcripts and diplomas",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validationHints: ["Most recent institution transcripts", "Translated to English if needed"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "test-scores",
      label: "Standardized test scores (TOEFL/IELTS/GRE/SAT)",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "financial-proof",
      label: "Financial proof for first year of study",
      description: "Bank statements, scholarship letters, sponsor affidavits.",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validationHints: [
        "Covers tuition + living expenses for the first year",
        "Account holder matches applicant or sponsor",
      ],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
    {
      id: "ties-home",
      label: "Proof of ties to home country",
      required: true,
      uploadable: true,
      acceptedMimeTypes: ["application/pdf", "image/*"],
      validation: { maxBytes: 10 * 1024 * 1024, mimeTypes: ["application/pdf", "image/*"] },
    },
  ],
};
